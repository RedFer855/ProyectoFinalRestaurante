<?xml version="1.0" encoding="utf-8"?>
<!--
  Pantalla principal con menú hamburguesa (DrawerLayout + NavigationView).
  Los ítems se filtran por rol en MainActivity. Los módulos no construidos
  muestran un placeholder en el contenedor de contenido (Plan Fase 1b, E6).
-->
<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main_drawer"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <LinearLayout
        android:id="@+id/main_content"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <!--
          Altura wrap_content + minHeight, NO ?attr/actionBarSize fijo: MainActivity
          le aplica el inset superior como padding (edge-to-edge), y sobre una altura
          fija ese padding recorta el ícono de hamburguesa y el título fuera de la caja.
          Con wrap_content la barra crece para alojar el inset.
        -->
        <com.google.android.material.appbar.MaterialToolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:minHeight="?attr/actionBarSize"
            android:background="?attr/colorPrimary"
            android:titleTextColor="?attr/colorOnPrimary"
            app:title="@string/app_name" />

        <!--
          Contenedor único de módulos: cada ítem del menú reemplaza el Fragment de acá
          dentro, sin abrir Activities nuevas. Es lo que permite que las tres roles usen
          las mismas pantallas (Plan Fase 1c, Entregable 2).
        -->
        <androidx.fragment.app.FragmentContainerView
            android:id="@+id/contenedor_contenido"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1" />
    </LinearLayout>

    <com.google.android.material.navigation.NavigationView
        android:id="@+id/nav_view"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        app:headerLayout="@layout/nav_header"
        app:menu="@menu/menu_navegacion" />

</androidx.drawerlayout.widget.DrawerLayout>
